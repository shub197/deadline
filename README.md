# Deadline

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 20.0.5.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Changes to do

1. Replace baseUrl and endPoint in deadline.service.ts as per your need.
2. In deadline.ts file adjust path and queryParams according to your API.
3. In deadline.ts response is also handle you can adjusted according to your requirements and in deadline.html file too.

## What trying to do is

Just showing how to fetch data and fetch again after 1 second with structure code